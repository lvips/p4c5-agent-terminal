# P4C5 Mac 端工具依赖
#
# ASR Adapter (阿里云 NLS ISI, 与 OMT 同等配置):
#   pip3 install websockets numpy opuslib requests
#
# TTS Adapter (阿里百炼 DashScope CosyVoice):
#   pip3 install websockets numpy requests

websockets>=12.0
numpy>=1.24
requests>=2.31

# ASR 必需 (OPUS 解码)
opuslib>=3.0.5

# 注意: 如果你不想用真阿里云 API, 用 --mock 参数即可
# 真 API 调用需要以下环境变量:
#   export ALIYUN_ACCESS_KEY_ID=xxx
#   export ALIYUN_ACCESS_KEY_SECRET=xxx
#   export ALIYUN_NLS_APPKEY=xxx              # NLS 应用 appkey
#   export DASHSCOPE_API_KEY=sk-xxx          # 阿里百炼 API key